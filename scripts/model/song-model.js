class songModel {
    constructor(id, name,  artist, img, audio){
        this.id = id,
        this.name = name,
        this.artist = artist,
        this.img = img,
        this.audio = audio
    }
}

export default songModel;